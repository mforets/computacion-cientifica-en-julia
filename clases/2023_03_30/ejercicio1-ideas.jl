# Sea x un vector del largo deseado.
# Para cada i de 1 al largo de x
#   x[i] = obtener_valor()
# Devolver matriz de tamano deseado.
#
x = 1:100

# a <- numero aleatorio
# mientras que (a < 1) || (a > 10)
#   a <- numero aleatorio
# devolver a


reshape(x, (10, 10))

[
    x[1] x[4]
    x[2] x[5]
    x[3] x[6]
]
